/**
 * Animation-related constants used across the application
 */

/**
 * Maximum number of stagger animation classes available.
 * This value should match the CSS stagger classes defined in globals.css.
 * Used to cap animation indexes so items beyond this value animate together.
 */
export const MAX_STAGGER_INDEX = 12;
